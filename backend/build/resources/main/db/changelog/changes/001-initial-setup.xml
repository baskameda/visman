<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <!--
        Changeset 001: Application Baseline
        ====================================
        This is the application baseline changeset.

        Operaton creates its own tables (ACT_*, HI_*, etc.) automatically via
        its internal schema management (operaton.bpm.database.schema-update=true).

        Add application-specific tables in new changesets below this one.
    -->
    <changeSet id="001-baseline" author="visitor-poc">
        <comment>Baseline changeset – confirms Liquibase is operational for this database.</comment>

        <!--
            Example: future application-specific table.

        <createTable tableName="poc_visitor_audit">
            <column name="id"             type="BIGINT" autoIncrement="true"><constraints primaryKey="true" /></column>
            <column name="process_instance_id" type="NVARCHAR(64)" />
            <column name="visitor_name"   type="NVARCHAR(255)" />
            <column name="checked_in_at"  type="DATETIME2" />
        </createTable>
        -->
        <sql>SELECT 1</sql>
    </changeSet>

</databaseChangeLog>
